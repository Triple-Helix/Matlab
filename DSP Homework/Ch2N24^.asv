x = load('djw6576.txt');
x = [zeros(1,50) x zeros(1,50)]

n = 51
while n>50 && n<50+50+600+1
    t1 = x(n-50:n-0);
    a = sum(t1);
    y1 = [y1 a];
    n++;
end

n=51
lb = -25
ub = 25
while n>25 && n<50+50+600+1-25
    while lb<25
        t2 = x(n-25);
        b = sum(t2);
        b = b + t2;
        lb++;
    end
    y2 = [y2 b];
    lb = -25;
    n++;
end
